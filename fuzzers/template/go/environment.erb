source /root/fuzzer-files/base/environment
#### Required for build steps:
export BUILD_FILES="/root/fuzzer-files/<%= @fuzzer_name %>"

#### Required for run-time
export FUZZER_NAME="gofuzz-<%= @fuzzer_name %>"

#### gofuzz Settings: edit these to setup fuzzer
export GO_FUZZ_ZIP=$BUILD_FILES/ # FILL IN THIS PATH
